import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        primary: '#ffe600',
        secondary: '#FFFFFF',
        hover: '#0591AF',
        text: '#1A202C',
        input: '#718096',
    },
});

export default theme;
